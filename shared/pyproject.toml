[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "ohi-shared"
version = "0.1.0"
description = "Common utilities shared between OHI web and server services"
requires-python = ">=3.9"

[tool.setuptools]
packages = ["ohi_shared"]
